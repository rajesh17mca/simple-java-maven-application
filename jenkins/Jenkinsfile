pipeline {
  agent any
  // agent {
  //   label 'DevServer'
  // }

  // tools {
  //   maven 'mymaven'
  // }

  stages {
    stage('build') {
      steps {
        echo "This is Build stage"
        // sh 'mvn clean package'
      }

      // post {
      //   success {
      //     archiveArtifacts artifacts: '**/target/*.war'
      //   }
      // }
    }

    stage('test') {
      steps {
        echo "This is Testing stage"
      }
    }

    stage ('deploy') {
      steps {
        echi "Thhis is Deploy stage"
      }
    }
  }
}